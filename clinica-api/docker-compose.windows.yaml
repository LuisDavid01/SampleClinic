version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: fisio_postgres_windows
    environment:
      POSTGRES_DB: salena_fisio
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data_windows:/var/lib/postgresql/data
      - ./scripts/docker-init-mejorado.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - fisio_network

  api:
    build:
      context: .
      dockerfile: Dockerfile.windows
    container_name: fisio_api_windows
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY}
      - PRISMA_SKIP_POSTINSTALL_GENERATE=true
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./uploads:/app/uploads
      - ./logs:/app/logs
    networks:
      - fisio_network
    restart: unless-stopped

volumes:
  postgres_data_windows:

networks:
  fisio_network:
    driver: bridge
