'use client'
export const HeroSkeleton = () => {
   return (
      <div className="absolute top-0 left-0 w-full h-full 
      bg-gradient-to-r from-foreground/40 via-foreground/20 to-foreground/40 
      animate-pulse z-0">
      </div>

  );
};

